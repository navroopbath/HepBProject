!!! 5
%html
  %head
    %title HepBProject
    = stylesheet_link_tag 'application'
    = javascript_include_tag 'application'
    / = javascript_include_tag 'http://fullcalendar.io/js/fullcalendar-2.1.1/lib/moment.min.js'
    / = javascript_include_tag 'http://fullcalendar.io/js/fullcalendar-2.1.1/fullcalendar.min.js'
    = csrf_meta_tags

  %body
    - if flash[:error]
      %div#error-container
        -flash.each do |key, value|
          %div.alert.alert-danger
            %button.close{:type=>'button', 'data-dismiss'=>'alert'}
            = value
      #container
        = yield
    - else
      %div#flash-container
        -flash.each do |key, value|
          %div.alert.alert-danger
            %button.close{:type=>'button', 'data-dismiss'=>'alert'}
            = value
      #container
        = yield